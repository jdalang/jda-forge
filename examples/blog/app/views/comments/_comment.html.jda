<% fn tmpl_comment_row(comment: &CommentRow) %>
<div class="comment">
  <strong><%= comment.author %></strong> <span class="meta"><%== comment.created_at %></span>
  <p><%= comment.body %></p>
</div>
