# Copy this file to .env.development / .env.staging / .env.production
# and fill in real values. Never commit files with real secrets.

DATABASE_URL=postgres://postgres:postgres@localhost/blog_dev
SMTP_HOST=
SMTP_USER=
SMTP_PASS=
APP_SECRET=change-me-at-least-32-chars-long
APP_PORT=8080
